# Ride-Link
•Built a full-stack hybrid mobility platform featuring a route-overlap matching algorithm that assigns a single driver to simultaneous passenger and parcel requests, reducing redundant vehicle dispatch; load-tested to 1,000 concurrent users with simulation scripts across admin, driver, and user workflows.
